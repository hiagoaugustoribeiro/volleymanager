import React from "react";
import AdminPanel from "./AdminPanel"; // Importa o painel administrativo

function App() {
  return (
    <div>
      <AdminPanel /> {/* Renderiza o painel no App */}
    </div>
  );
}

export default App;
